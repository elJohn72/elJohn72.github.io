---
import type { Locale } from '../i18n/ui';
import { getUi, localePath } from '../i18n/ui';
import LocaleSwitcher from './LocaleSwitcher.astro';

interface Props {
  locale: Locale;
  pathname: string;
}

const { locale, pathname } = Astro.props;
const t = getUi(locale);

const links =
  locale === 'es'
    ? [
        { href: localePath('es', '/'), label: t.nav.home },
        { href: localePath('es', '/proyectos'), label: t.nav.projects },
        { href: localePath('es', '/sobre-mi'), label: t.nav.about },
        { href: localePath('es', '/cv'), label: t.nav.cv },
        { href: localePath('es', '/contacto'), label: t.nav.contact },
      ]
    : [
        { href: localePath('en', '/'), label: t.nav.home },
        { href: localePath('en', '/projects'), label: t.nav.projects },
        { href: localePath('en', '/about'), label: t.nav.about },
        { href: localePath('en', '/cv'), label: t.nav.cv },
        { href: localePath('en', '/contact'), label: t.nav.contact },
      ];

const isActive = (href: string) =>
  pathname === href || (href !== `/${locale}/` && pathname.startsWith(href));
---

<header class="sticky top-0 z-50 border-b border-slate-800/80 bg-ink-950/70 backdrop-blur-xl">
  <div class="mx-auto flex max-w-6xl items-center justify-between gap-4 px-4 py-3 md:px-6">
    <a href={localePath(locale, '/')} class="group shrink-0 transition opacity-90 hover:opacity-100">
      <img
        src="/images/logo.svg"
        alt="eljhon72"
        width="200"
        height="36"
        class="h-8 w-auto md:h-9"
        loading="eager"
        decoding="async"
      />
    </a>
    <nav class="hidden items-center gap-1 md:flex" aria-label="Main">
      {links.map((link) => (
        <a
          href={link.href}
          class:list={[
            'rounded-lg px-3 py-2 font-mono text-xs transition',
            isActive(link.href)
              ? 'border border-cyan-500/30 bg-cyan-500/10 text-cyan-300'
              : 'text-slate-400 hover:bg-slate-800/50 hover:text-slate-200',
          ]}
        >
          {link.label}
        </a>
      ))}
    </nav>
    <div class="flex items-center gap-2">
      <LocaleSwitcher locale={locale} pathname={pathname} />
    </div>
  </div>
  <nav class="flex gap-2 overflow-x-auto border-t border-slate-800/80 px-4 py-2 md:hidden" aria-label="Mobile">
    {links.map((link) => (
      <a
        href={link.href}
        class:list={[
          'whitespace-nowrap rounded px-2 py-1 font-mono text-xs',
          isActive(link.href) ? 'text-cyan-400' : 'text-slate-500',
        ]}
      >
        {link.label}
      </a>
    ))}
  </nav>
</header>
